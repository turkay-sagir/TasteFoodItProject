@model TasteFoodIt.Entities.SocialMedia
@{
    ViewBag.Title = "CreateSocialMedia";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="container-fluid" id="container-wrapper">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Sosyal Medya Ekleme Sayfası</h1>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="./">Ana Sayfa</a></li>
            <li class="breadcrumb-item">Kategoriler</li>
            <li class="breadcrumb-item active" aria-current="page">Kategori İşlemleri</li>
        </ol>
    </div>

    <div class="row">
        <div class="col-lg-12">
            <!-- Form Basic -->
            <div class="card mb-4">
                <div class="card-body">
                    @using (Html.BeginForm("CreateSocialMedia", "SocialMedia", FormMethod.Post, new { enctype = "form-data/multipart" }))
                    {
                        <form>
                            <div class="form-group">
                                <label>Platform Adı</label>
                                @Html.TextBoxFor(x => x.PlatformName, new { @class = "form-control" })
                            </div>

                            <div class="form-group">
                                <label>Platform Url</label>
                                @Html.TextBoxFor(x => x.RedirectUrl, new { @class = "form-control" })
                            </div>

                            <div class="form-group">
                                <label>İkon Url</label>
                                @Html.TextBoxFor(x => x.IconUrl, new { @class = "form-control" })
                            <small>fa fa-SosyalMedyaAdı şeklinde giriş yapınız!</small>
                            </div>

                            <button type="submit" class="btn btn-primary">Sosyal Medya Ekle</button>
                        </form>
                    }
                </div>
            </div>

        </div>
    </div>
</div>

